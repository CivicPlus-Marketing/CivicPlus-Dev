import type { Context } from '@roots/bud-framework/options';
import { filesystem } from '@roots/bud-support/utilities/filesystem';
import type { Cli } from './app.js';
/**
 * Command finder class
 */
export declare class Commands {
    context: Partial<Context>;
    application: Cli;
    static instance: Commands;
    fs: typeof filesystem;
    paths: Array<string>;
    /**
     */
    private constructor();
    /**
     * @public
     * @static
     */
    static get(application: Cli, context: Partial<Context>): Commands;
    static importCommandsFromPaths(paths: Array<string>): Promise<any>;
    /**
     * Find commands shipped with a given extension
     */
    findExtensionCommandPaths(paths: Array<string>): string[];
    /**
     * Get commands
     *
     * @remarks
     * Returns cached commands if they exist, otherwise
     * resolves and caches commands from project dependencies.
     */
    getCommands(): Promise<any[]>;
    /**
     * Get array of project dependency and devDependency signifiers
     */
    getProjectDependencySignifiers(): Array<string>;
    /**
     */
    getRegistrationModulePaths(): Promise<Array<any>>;
    resolveExtensionCommandPaths(paths: Array<string>): Promise<string[]>;
}
