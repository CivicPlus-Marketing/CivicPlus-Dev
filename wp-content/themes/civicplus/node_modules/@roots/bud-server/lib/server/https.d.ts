/// <reference types="node" resolution-mode="require"/>
/// <reference types="node" resolution-mode="require"/>
/// <reference types="express-serve-static-core" resolution-mode="require"/>
import type { Connection } from '@roots/bud-framework/services/server';
import type { RequestListener } from 'node:http';
import { BaseServer } from '@roots/bud-server/server/base';
import { Server as HttpsServer } from 'node:https';
/**
 * HTTPS Server
 */
export declare class Server extends BaseServer implements Connection {
    /**
     * Server instance
     */
    instance: HttpsServer;
    /**
     * createServer
     */
    createServer(express: RequestListener & Express.Application): Promise<HttpsServer>;
}
