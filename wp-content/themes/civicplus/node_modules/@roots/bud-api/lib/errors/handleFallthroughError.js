import { InputError } from '@roots/bud-support/errors';
/**
 * Handle fallthrough
 */
export async function handleFallthroughError(bud, label, errorOptions = {}) {
    throw new InputError(`Invalid input in ${label}`, errorOptions);
}
