import { jsx as _jsx } from "react/jsx-runtime";
import { Box, Static } from '@roots/bud-support/ink';
import { Log } from './log.js';
export const Console = ({ messages }) => {
    if (!messages?.length)
        return null;
    return (_jsx(Static, { items: messages, children: (message, i) => (_jsx(Box, { flexDirection: "column", children: _jsx(Log, { ...message }, i) }, i)) }));
};
