interface Props {
    devUrl?: URL;
    displayServerInfo: boolean;
    proxyUrl?: URL;
    publicDevUrl?: URL;
    publicProxyUrl?: URL;
    watchFiles: Set<string>;
}
/**
 * Server info ink component
 */
export declare const Server: ({ devUrl, displayServerInfo, proxyUrl, publicDevUrl, publicProxyUrl, watchFiles, }: Props) => import("react/jsx-runtime").JSX.Element;
export {};
