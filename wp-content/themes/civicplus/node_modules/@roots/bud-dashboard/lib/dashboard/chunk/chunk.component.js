import { jsx as _jsx } from "react/jsx-runtime";
import { Box } from '@roots/bud-support/ink';
import { longestAssetNameLength } from '../format.js';
import Asset from './asset.component.js';
const Chunk = ({ final, indent, minWidth, ...chunk }) => {
    return (_jsx(Box, { flexDirection: "column", children: chunk?.assets?.map((asset, index) => (_jsx(Asset, { ...asset, emitted: chunk?.emitted, final: index == chunk?.assets.length - 1, indent: [!final], minWidth: longestAssetNameLength(chunk?.assets) }, index))) }));
};
export default Chunk;
