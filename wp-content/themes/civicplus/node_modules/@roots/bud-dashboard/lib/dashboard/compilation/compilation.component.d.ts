import type { StatsCompilation } from '@roots/bud-framework/config';
import type { Context } from '@roots/bud-framework/options/context';
interface Props {
    compilation: StatsCompilation;
    context: Context;
    displayAssets: boolean;
    displayEntrypoints: boolean;
}
declare const Compilation: ({ compilation, context, displayAssets, displayEntrypoints, }: Props) => import("react/jsx-runtime").JSX.Element;
export default Compilation;
