import type { Bud } from '../index.js';
import type { File } from '../types/options/context.js';
/**
 * User config parser
 */
declare class Configuration {
    bud: Bud;
    /**
     * Class constructor
     */
    constructor(bud: Bud);
    /**
     * Process dynamic configuration
     */
    dynamicConfig(description: any): Promise<unknown>;
    handleConfigEntry(obj: any, [key, value]: [string, unknown]): Promise<void>;
    /**
     * Process configuration
     */
    run(description: File): Promise<unknown>;
    /**
     * Process static configuration
     */
    staticConfig(description: File): Promise<unknown>;
}
export default Configuration;
