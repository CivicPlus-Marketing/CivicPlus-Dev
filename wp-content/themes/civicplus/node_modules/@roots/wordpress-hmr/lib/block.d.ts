import type { BlockConfiguration } from '@wordpress/blocks';
import type * as Filter from './filter.js';
import type { Style } from './style.js';
import type { Variant } from './variation.js';
export interface Props extends BlockConfiguration<Record<string, any>> {
    filters?: Filter.KeyedFilters;
    name: string;
    styles?: Array<Omit<Style, 'block'>>;
    variations?: Array<Omit<Variant, 'block'>>;
}
/**
 * Register block
 */
export declare const register: ({ filters, name, styles, variations, ...settings }: Props) => void;
/**
 * Unregister block
 */
export declare const unregister: ({ filters, name, styles, variations, }: Pick<Props, `filters` | `name` | `styles` | `variations`>) => void;
