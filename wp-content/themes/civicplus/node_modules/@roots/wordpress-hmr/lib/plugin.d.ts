import type { WPPlugin } from '@wordpress/plugins';
import type * as Filter from './filter.js';
export interface Props extends WPPlugin {
    filters?: Filter.KeyedFilters;
    name: string;
    settings: Omit<WPPlugin, `name`>;
}
export declare const isRegistered: (name: string) => boolean;
export declare const register: ({ filters, name, ...settings }: Props) => void;
export declare const unregister: ({ filters, name }: {
    filters: any;
    name: any;
}) => void;
