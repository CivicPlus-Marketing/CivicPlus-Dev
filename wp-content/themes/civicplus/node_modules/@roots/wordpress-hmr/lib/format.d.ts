export interface Props {
    className: string;
    edit: CallableFunction;
    name: string;
    tagName: string;
    title: string;
}
export declare const isRegistered: (name: string) => any;
export declare const register: ({ name, ...settings }: Props) => void;
export declare const unregister: ({ name }: {
    name: any;
}) => void;
